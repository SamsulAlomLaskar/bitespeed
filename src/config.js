// Define supported node types for the canvas
export const NODE_TYPES = {
    TEXT: 'text', // Represents a text message node
};

// Map each node type to a human-readable label for UI display
export const NODE_LABELS = {
    [NODE_TYPES.TEXT]: 'Text Message', // Label shown for text nodes in the UI
};
